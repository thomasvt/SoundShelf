<Window x:Class="SoundShelf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoundShelf"
        xmlns:waveForm="clr-namespace:SoundShelf.WaveForm"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Loaded="MainWindow_OnLoaded">
    <Grid>
        <DockPanel Margin="10">

            <!-- top stuff -->
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical">
                <TextBox Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top"></TextBox>
            </StackPanel>

            <!-- bottom stuff -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical">
                <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical">
                    <Label Content="{Binding CountLabel}"></Label>

                    <waveForm:WaveFormViewer DockPanel.Dock="Bottom" x:Name="WaveformViewer" Height="100" Background="#222" />

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="▶" Padding="8" Click="PlayButton_Click"></Button>
                        <Button Content="◼" Padding="8"></Button>
                        <Button Content="Export selection" Padding="8"></Button>
                    </StackPanel>

                    <DockPanel >
                        <Button DockPanel.Dock="Right" Content="..." Padding="4" Margin="4" Click="ConfigureLibrary_Click"/>
                        <Button Content="Refresh library" Padding="4"  Margin="4" Click="RefreshLibrary_Click"/>
                    </DockPanel>
                    <ProgressBar Height="8" Maximum="{Binding ScanTaskTotal}" Value="{Binding ScanTaskDone}" />
                </StackPanel>

            </StackPanel>

            <!-- Sound search results -->
            <ListBox ItemsSource="{Binding Results}"
                     SelectionChanged="SoundList_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:SoundFileInfo}">
                        <StackPanel>
                            <TextBlock Text="{Binding FileName}" FontWeight="Bold"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </DockPanel>
    </Grid>
</Window>
